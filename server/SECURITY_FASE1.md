The user says: "Apply the SUGGESTED EDIT to the ORIGINAL CODE. Output the complete modified file."
# Fase 1 – Segurança Básica (Concluída)

## ✅ Alterações Implementadas

### 1. Trocar MD5 por bcrypt

**Arquivo:** `includes/expressRoutes.js`

- **Login agora usa `bcrypt.compare()`** em vez de MD5

- **Tokens de sessão** ger com `crypto.randomBytes(32)` (256-bit) em vez de MD5
- Removida dependência de `crypto.createHash("md5")` para senhas e tokens

**Script de Migração:**

- Criado `scripts/migrate-password.js` para migrar senhas antigas ( MD5 → bcrypt)
- Uso:















































































































